export const classes = `interface TextFieldClasses {
  label?: string
  input?: string
}`

export const styles = `interface TextFieldStyles {
  label?: React.CSSProperties
  input?: React.CSSProperties
}`
