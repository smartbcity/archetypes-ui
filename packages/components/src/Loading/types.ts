export const classes = `interface LoadingClasses {
  iconContainer?: string
  linearProgress?: string
}`

export const styles = `interface LoadingStyles {
  iconContainer?: React.CSSProperties
  linearProgress?:  React.CSSProperties
}`
