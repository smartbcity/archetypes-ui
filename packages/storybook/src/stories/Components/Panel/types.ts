export const types = `interface PanelClasses {
    header?: string
    content?: string
    footer?: string
}
  
interface PanelStyles {
    header?: React.CSSProperties
    content?: React.CSSProperties
    footer?: React.CSSProperties
}`
