import Preview from '../../../Docs/Preview/Preview'
import {
  SBButton,
  ThemeContextProvider
} from '@smartb/archetypes-ui-components'
import PropsTable from '../../../Docs/PropsTable'
import { myTheme } from '../../../Docs/Theme/Theme'

<Meta title='Overview|Components/SBButton' />

# SmartB Button

<ThemeContextProvider theme={myTheme}>
  <SBButton style={{ margin: '20px auto', width: 'auto', display: 'block' }}>
    Click me
  </SBButton>
</ThemeContextProvider>

## Props

<PropsTable
  rows={[
    {
      name: 'className',
      type: 'string',
      defaultValue: '',
      required: false,
      description: 'The class added to the root element of the component'
    },
    {
      name: 'style',
      type: 'React.CSSProperties',
      defaultValue: '',
      required: false,
      description: 'The styles added to the root element of the component'
    },
    {
      name: 'id',
      type: 'string',
      defaultValue: '',
      required: false,
      description: 'The id added to the root element of the component'
    },
    {
      name: 'children',
      type: 'React.ReactNode',
      defaultValue: '',
      required: false,
      description: 'The react children props'
    },
    {
      name: 'onClick',
      type: '(event: React.ChangeEvent<{}>) => void',
      defaultValue: '',
      required: false,
      description: ''
    },
    {
      name: 'disabled',
      type: 'boolean',
      defaultValue: 'false',
      required: false,
      description: 'If true the button will be disabled'
    },
    {
      name: 'hoverEffect',
      type: 'boolean',
      defaultValue: 'false',
      required: false,
      description: 'If false the hoverEffect will be disabled'
    }
  ]}
/>

## Examples

<ThemeContextProvider theme={myTheme}>
  <Preview
    code={`<SBButton>Normal</SBButton>
<SBButton hoverEffect={false}>Without hoverEffect</SBButton>
<SBButton disabled={true}>Disabled</SBButton>`}
  >
    <SBButton>Normal</SBButton>
    <SBButton hoverEffect={false}>Without hoverEffect</SBButton>
    <SBButton disabled={true}>Disabled</SBButton>
  </Preview>
</ThemeContextProvider>
