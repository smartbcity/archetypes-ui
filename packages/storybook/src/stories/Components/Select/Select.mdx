import Preview from '../../../Docs/Preview/Preview'
import { PopUp, ThemeContextProvider } from '@smartb/archetypes-ui-components'
import PropsTable from '../../../Docs/PropsTable'
import { myTheme } from '../../../Docs/Theme/Theme'
import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter'
import { tomorrow } from 'react-syntax-highlighter/dist/esm/styles/prism'
import { types } from './types'

# SmartB PopUp

## Props

<PropsTable
  rows={[
    {
      name: 'className',
      type: 'string',
      defaultValue: '',
      required: false,
      description: 'The class added to the root element of the component'
    },
    {
      name: 'style',
      type: 'React.CSSProperties',
      defaultValue: '',
      required: false,
      description: 'The styles added to the root element of the component'
    },
    {
      name: 'id',
      type: 'string',
      defaultValue: '',
      required: false,
      description: 'The id added to the root element of the component'
    },
    {
      name: 'label',
      type: 'string',
      defaultValue: '',
      required: true,
      description: 'The label of the select'
    },
    {
      name: 'value',
      type: 'string',
      defaultValue: '',
      required: false,
      description: 'The current selected value also included in the items'
    },
    {
      name: 'disabled',
      type: 'boolean',
      defaultValue: 'false',
      required: false,
      description: 'Define if the component is disabled or not'
    },
    {
      name: 'items',
      type: 'SelectItem[]*',
      defaultValue: '',
      required: true,
      description: 'The title thart will be displayed at the top of the pop-up'
    },
    {
      name: 'onChange',
      type:
        '(event: React.ChangeEvent<{ name?: string; value: unknown }>, child: React.ReactNode) => void',
      defaultValue: '',
      required: false,
      description: 'The event called when a different item is selected'
    },
    {
      name: 'classes',
      type: 'SelectClasses*',
      defaultValue: '',
      required: false,
      description: 'The classes applied to the different part of the component'
    },
    {
      name: 'styles',
      type: 'SelectStyles*',
      defaultValue: '',
      required: false,
      description: 'The styles applied to the different part of the component'
    }
  ]}
/>

## Complexe Types

<SyntaxHighlighter language='typescript' style={tomorrow}>
  {types}
</SyntaxHighlighter>{' '}
