export const types = `interface SelectItem {
    value: string
    label: string
  }
  
  interface SelectClasses {
    label?: string
    select?: string
  }
  
  interface SelectStyles {
    label?: React.CSSProperties
    select?: React.CSSProperties
  }
  `
