import Preview from '../../../Docs/Preview/Preview'
import { ToolsPanel } from '@smartb/archetypes-ui-layout'
import PropsTable from '../../../Docs/PropsTable'
import {
  OneSectionMenu,
  MultipleSectionMenu
} from '../../../Docs/Variables/IconProfile.tsx'
import { Prism as SyntaxHighlighter } from 'react-syntax-highlighter'
import { tomorrow } from 'react-syntax-highlighter/dist/esm/styles/prism'
import { types } from './types'

<Meta title='Overview|Layout/ToolsPanel' />

# SmartB ToolsPanel

<ToolsPanel
  style={{ display: 'block', margin: 'auto' }}
  menu={OneSectionMenu}
/>

## Props

<PropsTable
  rows={[
    {
      name: 'className',
      type: 'string',
      defaultValue: '',
      required: false,
      description: 'The class added to the root element of the component'
    },
    {
      name: 'style',
      type: 'React.CSSProperties',
      defaultValue: '',
      required: false,
      description: 'The styles added to the root element of the component'
    },
    {
      name: 'id',
      type: 'string',
      defaultValue: '',
      required: false,
      description: 'The id added to the root element of the component'
    },
    {
      name: 'menu',
      type: 'Menu',
      defaultValue: '',
      required: true,
      description:
        'the menu that contains all the items that will be displayed in the panel'
    }
  ]}
/>

The component ToolsPanel hasn't the props classes and styles because he is recursive.

## Complexe Types

<SyntaxHighlighter language='typescript' style={tomorrow}>
  {types}
</SyntaxHighlighter>

## Examples

### with sections

<Preview
  code={`<ToolsPanel
  style={{ display: 'block', margin: 'auto' }}
  menu={MultipleSectionMenu}
/>`}
>
  <ToolsPanel
    style={{ display: 'block', margin: 'auto' }}
    menu={MultipleSectionMenu}
  />
</Preview>

### without sections

<Preview
  code={`<ToolsPanel
  style={{ display: 'block', margin: 'auto' }}
  menu={OneSectionMenu}
/>`}
>
  <ToolsPanel
    style={{ display: 'block', margin: 'auto' }}
    menu={OneSectionMenu}
  />
</Preview>
