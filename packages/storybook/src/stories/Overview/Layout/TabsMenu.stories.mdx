import { Meta, Props, Story, Preview } from '@storybook/addon-docs/blocks'
import PropsTable from '../../../Docs/PropsTable'
import { TabsMenu } from '@smartb/archetypes-ui-layout'
import { AccountCircle } from '@material-ui/icons'

<Meta title='Overview|Layout/TabsMenu' />

# SmartB TabsMenu

<TabsMenu
  tabs={[{ label: 'section1' }, { label: 'section2' }]}
  variant='fixedWidth'
>
  <div style={{ width: '50px', height: '50px' }}>section1</div>
  <div style={{ width: '50px', height: '50px' }}>section2</div>
</TabsMenu>

## Props

<PropsTable
  rows={[
    {
      name: 'tabs',
      type: '[{label: string, icon?: JSX.Element}]',
      defaultValue: '',
      required: true,
      description:
        'Tabs contains an array of string and icons to defined the every section of the menu'
    },
    {
      name: 'children',
      type: 'React.ReactNode[]',
      defaultValue: '',
      required: true,
      description: 'Every nodes will fill the tab at the same index'
    },
    {
      name: 'variant',
      type: "'fullWidth' | 'fixedWidth'",
      defaultValue: "'fullWidth'",
      required: false,
      description:
        "If 'fullWidth' the tabs will take the all the space of the parent if 'fixedWidth' every tab will have 100px of width"
    },
    {
      name: 'className',
      type: 'string',
      defaultValue: '',
      required: false,
      description:
        'Add the className to the container and override the default properties'
    },
    {
      name: 'style',
      type: 'React.CSSProperties',
      defaultValue: '',
      required: false,
      description:
        'Add the style to the container and override the default properties'
    }
  ]}
/>

## Examples

### fullWidth

<Preview>
  <TabsMenu
    tabs={[{ label: 'section1' }, { label: 'section2' }]}
    variant='fullWidth'
  >
    <div style={{ width: '50px', height: '50px' }}>section1</div>
    <div style={{ width: '50px', height: '50px' }}>section2</div>
  </TabsMenu>
</Preview>

### fixedWidth

<Preview>
  <TabsMenu
    tabs={[{ label: 'section1' }, { label: 'section2' }]}
    variant='fixedWidth'
  >
    <div style={{ width: '50px', height: '50px' }}>section1</div>
    <div style={{ width: '50px', height: '50px' }}>section2</div>
  </TabsMenu>
</Preview>

### with icons

<Preview>
  <TabsMenu
    tabs={[
      { label: 'section1', icon: <AccountCircle /> },
      { label: 'section2', icon: <AccountCircle /> }
    ]}
    variant='fixedWidth'
  >
    <div style={{ width: '50px', height: '50px' }}>section1</div>
    <div style={{ width: '50px', height: '50px' }}>section2</div>
  </TabsMenu>
</Preview>
