export const classes = `interface PageClasses {
  header: string
  backButton: string
}`
export const styles = `interface PageStyles {
  header: React.CSSProperties
  backButton: React.CSSProperties
}`
