export const classes = `interface AppBarLayoutClasses {
  menuButton?: string
  menuIcon?: string
  toolBar?: string
}`
export const styles = `interface AppBarLayoutStyles {
  menuIcon?: React.CSSProperties
  toolBar?: React.CSSProperties
}`
