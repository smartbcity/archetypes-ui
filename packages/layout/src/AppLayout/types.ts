export const classes = `interface AppLayoutClasses {
  main?: string
  content?: string
}`
export const styles = `interface AppLayoutStyles {
  main?: React.CSSProperties
  content?: React.CSSProperties
}`
export const StyleProps = `interface StyleProps {
  detailDrawerWidth: number;
  appBarHeight: number;
  menuWidth: number;
}`
